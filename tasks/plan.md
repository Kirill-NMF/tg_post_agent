# Implementation Plan

Canonical plan: docs/project-spec/DEVELOPMENT_PLAN.md.

Current stage: Phase 12 Real Formatting Adapter public flow is implemented with Tier 1 deterministic evidence. Stage 3 Telegram transport remains not tested; Premium/custom emoji remain deferred.

Next gate: configure the owner-approved OpenRouter formatting model through the VPS-only runtime environment, controlled-deploy one poller/worker, then run exactly two bounded Tier 2 dedicated-chat canaries (Option 1 and Option 2, one model call each, no retry/fallback). Only after those pass is owner 3/3 Option 1/Option 2 literary/UX review applicable.
